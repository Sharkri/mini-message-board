extends layout

block content
  h1= "Submit a message"
  form.create-message(action="/new", method="POST")
    label
      | Your Name:
      input(type="text", name="name")

    label
      | Message:
      textarea(name="message", rows=3)

    button(type="submit") Submit message
    a(href="/") Go back
